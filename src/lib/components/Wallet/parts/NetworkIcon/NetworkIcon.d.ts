import { FC } from "react";
import { ChainIconSize } from "../ChainIcon/ChainIcon";
interface NetworkIconProps {
    size: ChainIconSize;
    className?: string;
}
declare const NetworkIcon: FC<NetworkIconProps>;
export default NetworkIcon;
